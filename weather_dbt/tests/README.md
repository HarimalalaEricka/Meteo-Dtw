# Tests

dbt tests validate data quality and integrity.

**Test Types:**
- Generic tests (not null, unique, relationships)
- Custom tests via `.yml` files

**Usage:**
- Run tests: `dbt test`
- Validate data contracts before downstream use
